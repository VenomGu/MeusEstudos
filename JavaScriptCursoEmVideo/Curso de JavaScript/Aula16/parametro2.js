function soma (n1=0,n2=0){ //Neste caso foi colocado o valor 0 nos valores do array, para garantir que o valor seja somado com algum valor na hora da chamada. 
    return n1 + n2
} // Toda essa função serve para somar o valor que está lá embaixo, isso pode facilitar na hora de preencher um local onde a pessoa quer somar, mas serve pra várias coisas pra facilitar o uso da função. 
console.log(soma(7,5)) //Chamada. 